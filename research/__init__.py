"""Research: factor-combination / crash-fix validation scripts, the shared return- and
metric-computation helpers (monthly L/S returns, passive benchmarks, vol-matching) that
the strategy and walk-forward layers reuse, and the CPCV / Deflated Sharpe / PBO
overfitting checks (``cpcv.py``) that sit on top of all of it.
"""
