# Core dependencies for pit-backtester.
# After your first install, you can pin exact versions with: pip freeze > requirements.txt

# Data
yfinance                # free-data comparison universes + the SPY cache
nasdaq-data-link        # Sharadar point-in-time prices + fundamentals (imports as `nasdaqdatalink`)
pyarrow                 # parquet caches (backtest/cache, data/cache/sharadar)

# Analysis
pandas
numpy
scipy                   # research/cpcv.py: skew, kurtosis, the normal CDF

# Broker / execution
ib_async

# Config
python-dotenv
